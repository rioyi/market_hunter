<%= form_with(model: unit_measurement, local: true) do |form| %>
  <% if unit_measurement.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(unit_measurement.errors.count, "error") %> prohibited this unit_measurement from being saved:</h2>

      <ul>
        <% unit_measurement.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :unit_name %>
    <%= form.text_field :unit_name %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_field :description %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
